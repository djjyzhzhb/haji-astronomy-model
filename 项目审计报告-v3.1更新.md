# 项目审计报告

## Vite+React+Three.js 天象推演系统 —— 项目上下文提取与审计（v3.1 对齐版）

> 审计日期：2026-06-09  
> 项目路径：`e:\哈吉语创制计划\天文模型\`  
> 审计范围：全部 `src/` 源码 + 配置文件 + 已有 `astronomy.ts` 天文模块  
> 注意：此报告是上一版审计报告的更新版本，反映当前代码最新状态（时间系统已统一、新增 astronomy.ts 等）

---

## 1. 技术栈确认

| # | 问题 | 回答 | 证据 |
|---|------|------|------|
| 1 | Vite 版本 | **v6.3.5** | `package.json`: `"vite": "^6.3.5"` |
| 2 | React 版本 | **18.3.1** | `package.json`: `"react": "^18.3.1"`, `"react-dom": "^18.3.1"` |
| 3 | Three.js 版本 | **0.172.0** | `package.json`: `"three": "^0.172.0"`, `"@types/three": "^0.172.0"` |
| 4 | 状态管理方案 | **Zustand v5.0.3** | `package.json`: `"zustand": "^5.0.3"`；单文件 Store：`src/store/index.ts` |
| 5 | React Three Fiber (R3F) | **有**：`@react-three/fiber ^8.17.10`，`@react-three/drei ^9.120.0` | `src/App.tsx` 使用 `<Canvas>`；组件中使用 `useFrame` |
| 6 | TypeScript | **是**，`typescript ~5.8.3`，**严格模式已开启** (`"strict": true`) | `tsconfig.json` L19 |
| 7 | UI 组件库 | **Tailwind CSS 3.4.17 + lucide-react 0.511.0** | `package.json` + `tailwind.config.js`；纯自研组件，无 Ant Design/MUI |
| 8 | 地图/观察点面板 | **纯 DOM Canvas 自研**（等距矩形投影 + Perlin 噪声昼夜/云层叠加层） | `MapPanel.tsx` |

---

## 2. 文件结构审计

```
src/
├── App.tsx                              ← 应用入口，R3F <Canvas> + SceneWrapper + 路由解析 + 移动端适配
│
├── components/
│   ├── Scene.tsx                        ← 主场景核心：useFrame 驱动天体位置更新（直接操作 ref，非 React state）
│   ├── Star.tsx                         ← 恒星：Mesh + PointLight×2 + 四层光晕 Mesh
│   ├── Planet.tsx                       ← 行星：Mesh(SphereGeometry) + RingGeometry×3 行星环 + 大气层 BackSide 球壳 + 卫星子组件
│   ├── Moon.tsx                         ← 卫星：Mesh(SphereGeometry) + useFrame 轨道位置更新
│   ├── Orbit.tsx                        ← 轨道线：Line(BufferGeometry)，支持开普勒椭圆（getOrbitPointsFromT）或降级圆形
│   ├── AsteroidBelt.tsx                 ← 小行星带：Points(1500 粒子)，每帧更新粒子轨道角
│   ├── DustCloud.tsx                    ← 尘埃云：Points(800 粒子球壳)
│   ├── ControlPanel.tsx                 ← 底部控制栏：时间倍速/暂停/轨道显隐/缩放/聚焦/视觉选项
│   ├── InfoPanel.tsx                    ← 天体信息面板：描述/参数/聚焦/编辑天体
│   ├── DetailPage.tsx                   ← 行星细节页（~1500行）：轨道视角 Canvas + 地表视角 SurfaceView + 地图面板 + 自定义着色器(dayNight/cloud/ring/atmosphere) + 阴影贴图 + 环形粒子
│   ├── SurfaceView.tsx                  ← 地表天象视图（~1300行）：天空穹顶着色器 + 星空 + 太阳 Billboard + 天体标记(CelestialMarkers) + 轨迹预测(CelestialTrajectory) + 方向标识 + 地平线 + 黄道线 + ENU 坐标系
│   ├── MapPanel.tsx                     ← 地图侧边面板（~500行）：等距矩形投影 + 海陆检测 + 昼夜阴影 + Perlin 云层
│   └── PerformanceMonitor.tsx            ← FPS/帧时间/对象数监控
│
├── config/
│   ├── constants.ts                     ← 天文常数：D_YEAR, D_S, EPSILON_RAD, ECCENTRICITY, ARCSEC2RAD 等
│   └── celestialBodies.ts               ← 已废弃（空 export {}）
│
├── store/
│   └── index.ts                         ← Zustand store：天体数据 + TimeSystem(T/timeScale/isPaused) + 视觉参数 + DetailPageState + SurfaceObservation + CalendarSystem
│
├── types/
│   └── index.ts                         ← TypeScript 类型：CelestialBody, DetailPageState, SurfaceObservationState, TimeSystem, CalendarSystem, StoreState（含 OrbitalElements 引用 + TextureParams 引用）
│
├── utils/
│   ├── astronomy.ts                     ← **天文计算纯函数模块（新增）**：calcGMST, calcLST, sunEclipticLow/Medium/High, eclipticToEquatorial, equatorialToHorizontal, generateEclipticPoints, generateRingPoints, horizontalToWorldDir
│   ├── keplerOrbit.ts                   ← 开普勒轨道计算：solveKeplerEquation, convertOrbitalToCartesian, calculateOrbitalPositionFromT, calculateOrbitalPosition, getOrbitPoints, createSimpleOrbit
│   ├── calendar.ts                      ← 历法计算：calendarFromT, isLeapYear, yearLength（10个月 + 岁末补日 + 7年一闰）
│   ├── surfaceCoords.ts                 ← 地表坐标：calculateSunSkyPosition(@deprecated), worldToSkyPosition, altAzToDirection, applyAtmosphericRefraction
│   ├── router.ts                        ← Hash 路由：parseHash / setHash (#/detail/planetId)
│   ├── textureGenerator.ts              ← 程序化纹理生成：generateTerrainTexture, generateCloudTexture, generateAtmosphereTexture, 等
│   ├── planetTextureCache.ts            ← 贴图缓存：usePlanetTexture hook + Image 缓存 + 噪声处理
│   └── astronomy.test.ts                ← 天文模块验证用例（7 个测试，控制台运行）
│
├── main.tsx                             ← ReactDOM 入口
└── index.css                            ← Tailwind 全局样式
```

---

## 3. 坐标系与空间约定

| # | 问题 | 回答 | 代码证据 |
|---|------|------|----------|
| 1 | Y-up 还是 Z-up？ | **Y-up**（Three.js 默认） | `keplerOrbit.ts:49` 注释："轨道主要在 X-Z 平面（赤道平面），Y 为竖直轴"；`astronomy.ts:6` 注释："采用 Y-up 世界空间约定（Y=天极）" |
| 2 | 星球中心在原点？ | **恒星在原点 (0,0,0)**；行星/卫星按轨道偏移 | `Scene.tsx:33`：`positions.set(star.id, new THREE.Vector3(0, 0, 0))`；`Star.tsx:21`：`pointLight position={[0, 0, 0]}` |
| 3 | 半径单位？ | **抽象单位**，1 单位 ≈ 6000 km（设定推导） | `store/index.ts:47`：宜居行星 `radius: 2.0`，注释"略小于地球（12000km/6000=2.0）"；恒星 `radius: 8.0` |
| 4 | "北"对应哪个世界轴？ | **+Y 是自转轴/北极方向**（经轴倾角旋转后偏离纯 Y 轴） | `keplerOrbit.ts:62-63`：轨道面在 X-Z 平面；`surfaceCoords.ts:124`：`localY = planetRadius * sinLat`（南北方向/极轴）；`astronomy.ts:6`注释明确 |
| 5 | "东"对应哪个世界轴？ | **经度 0° = +Z**，**经度 +90° = +X（东）**——右手术 | `surfaceCoords.ts:123`：`localX = planetRadius * cosLat * sinLon`（东西方向, lon=π/2 → +X）；`surfaceCoords.ts:125`：`localZ = planetRadius * cosLat * cosLon`（lon=0 → +Z） |
| 6 | 相机默认位置？ | **太空轨道视角（俯视）** | `App.tsx:112`：`cameraPosition = [0, 30*distanceScale, 50*distanceScale]`；`DetailPage.tsx:699`：`position: [0, 0, 6]`（质量分段不同） |
| 7 | OrbitControls up 向量？ | **默认 (0,1,0)**（未显式设置 up prop） | `App.tsx:57`：`<OrbitControls>` 无 `up` 属性；`DetailPage.tsx:700`：同样无 `up` |
| 8 | 经纬度 → 世界坐标转换？ | **有**，`worldToSkyPosition()` in `surfaceCoords.ts:106-193` + `horizontalToWorldDir()` in `astronomy.ts:203-222` | 两套实现：`surfaceCoords.ts` 为完整轨道→地表投影；`astronomy.ts` 为纯天文坐标→世界方向 |

### 关键坐标约定总结

```
世界空间（Three.js 默认，Y-up）：
  +Y     = 上（行星自转轴/北极方向，经轴倾角旋转后）
  +X     = 东（经度 90°E）
  +Z     = 本初子午线（经度 0°）
  
  轨道平面 = X-Z 平面（Y=0）
  恒星     = 原点 (0,0,0)
  公转     = 绕 Y 轴在 X-Z 平面内（keplerOrbit.ts convertOrbitalToCartesian）

地表 ENU 局部坐标系（右手术）：
  天顶(up)     = 表面法线（从行星中心指向表面）
  东(east)     = rotationAxis × up
  北(north)    = up × east
  方位角 A     = 0°=北, 90°=东, 180°=南, 270°=西

天文坐标系（astronomy.ts）：
  黄道面     = 与赤道面夹角 EPSILON_RAD(12°)
  春分点     = T=0 时太阳在赤道上（δ=0, λ=0）
  黄经 λ     = 从春分点沿黄道向东测量
  赤经 α / 赤纬 δ = 标准赤道坐标
```

---

## 4. 时间系统审计

### 4.1 重大更新：时间已统一！

对比上一版审计报告，当前代码已完成时间系统统一。`Scene.tsx` 不再使用独立的 `timeRef`，而是通过 Zustand store 的 `timeSystem.T` 驱动所有时间相关计算。

### 4.2 时间状态存储

| 系统 | 存储位置 | 类型 | 作用域 |
|------|----------|------|--------|
| 统一时间 | `store.timeSystem.T` | Zustand（`number`） | **全局唯一**：主场景 + 详情页 + 地表视图全部使用同一时间源 |
| 时间参数 | `store.timeSystem` | `TimeSystem` 接口 | `T`(儒略日), `timeScale`(倍速), `isPaused`(暂停), `localDayHours`(24.15h), `localYearDays`(426.15) 等 |

### 4.3 时间变量类型与单位

```ts
// types/index.ts:51-59
export interface TimeSystem {
  T: number             // 儒略日（本初子午线春分正午=0，单位：本地日）
  timeScale: number     // 全局时间倍速
  isPaused: boolean     // 全局暂停
  localDayHours: number // 本地日长度 24.15h
  localYearDays: number // 本地年长度 426.15天
  localMonthDays: number // 本地月长度 42天
  mainMoonPeriodDays: number // 主卫星周期 41.3天
}
```

**时间累积方式**（`Scene.tsx:22-26`）：
```ts
useFrame((state, delta) => {
  if (!timeSystem.isPaused) {
    const dtDays = (delta * timeSystem.timeScale) / SECONDS_PER_DAY  // 帧秒 → 本地日
    updateTimeSystem({ T: timeSystem.T + dtDays })
  }
  // ... 后续使用 T 计算所有天体位置 ...
})
```

- 类型：`number`（自增儒略日）
- 单位：**本地日**（86400 地球秒 = 24h × 3600s/h，此处本地日为 24.15h × 3600 = 86940 秒）
- 基准点：**T=0 = 春分正午（太阳在赤道上，本初子午线处为正午 12:00）**
- 累积方式：`delta `→ `delta / SECONDS_PER_DAY`（帧秒 → 本地日）

### 4.4 详情页时间系统

详情页中同时存在 `detailPageState.globalTime`（秒）和 `detailPageState.dayTime`/`yearTime`（0-1 比例），但这些参数标记为 `@deprecated`。`SurfaceView.tsx` 中已改用 `store.timeSystem.T` 作为太阳位置计算输入：

```ts
// SurfaceView.tsx:1089-1096
const timeSystem = useStore(s => s.timeSystem)
const T = timeSystem.T
const [sunAltAstro, sunAzAstro] = useMemo(() => {
  const [sunLon, epsPrime] = sunEclipticHigh(T)
  const [sunRA, sunDec] = eclipticToEquatorial(sunLon, 0, epsPrime)
  const lst = calcLST(T, longitude)
  return equatorialToHorizontal(sunRA, sunDec, lst, latitude)
}, [T, longitude, latitude])
```

这说明地表视图已部分迁移到统一时间系统，但 `DetailPage.tsx` 的 PlanetMesh（轨道视角昼夜着色器）仍使用 `detailPageState.dayTime`：

```ts
// DetailPage.tsx (PlanetMesh useFrame):416-417 — 仍使用 dayTime
const angle = detailPageState.dayTime * Math.PI * 2
sunRef.current.set(Math.sin(angle), 0, Math.cos(angle))
```

### 4.5 时间流速控制

| 参数 | 位置 | 范围 | 说明 |
|------|------|------|------|
| `timeScale` | `store.timeSystem` | 0.1 ~ 100 | 控制全局时间流速倍数 |
| `isPaused` | `store.timeSystem` | boolean | 暂停全局时间 |
| `dayNightCycleSpeed` | `detailPageState` | 0~? | 控制详情页昼夜循环速度（**仍独立存在**） |

**不支持倒放**。`timeScale` 最小为 0.1。

### 4.6 位置计算方式

**每帧实时计算**，非预计算轨道点。有三层计算路径（优先级从高到低）：

1. **开普勒轨道 + 显式周期**：`calculateOrbitalPositionFromT(T, orbitalPeriodDays, orbitalElements)` —— 当 `body.orbitalPeriodDays` 存在时
2. **开普勒轨道 + 缩放周期**：`calculateOrbitalPositionScaled(T, orbitalElements, distanceScale, 50)` —— 当仅 `orbitalElements` 存在时
3. **降级圆形**：`angle = T * orbitSpeed * 0.1; x = cos(angle) * distance; z = sin(angle) * distance` —— 当无 `orbitalElements` 时

 جانب的位置以 `distanceScale` 缩放，然后写入 `bodyRefs` Map，在 `useFrame` 中直接操作 Three.js 对象：

```ts
// Scene.tsx:77-81（改进：不再使用 React state）
positions.forEach((pos, id) => {
  const obj = bodyRefs.current.get(id)
  if (obj) obj.position.copy(pos)
})
```

### 4.7 时间与观察者经度的绑定

**主场景：未绑定**（太空轨道视角无"本地时间"概念）。

**地表视图：已绑定**。通过 `calcLST(T, lonRad)` 将全局 T 与观察者经度转化为地方恒星时，再通过 `equatorialToHorizontal(ra, dec, lst, lat)` 计算太阳在地平坐标中的位置。这意味着不同经度的观测者会看到不同的太阳高度角/方位角。

---

## 5. 观察点系统审计

### 5.1 观察点数据存储

存于 Zustand store 的 `detailPageState.surfaceObservation`：

```ts
// types/index.ts:71-84
export interface SurfaceObservationState {
  latitude: number;              // 弧度
  longitude: number;             // 弧度
  isSurfaceView: boolean;        // 是否处于地表视角
  fov: number;                   // 视野角度
  atmosphereRefraction: boolean; // 大气折射开关
  refractionCoefficient: number; // 0.1°~2.0°
  markerSizeScale: number;       // 0.5x~3.0x
  showConstellations: boolean;
  constellationLineWidth: number;
  showEcliptic: boolean;
  eclipticLineWidth: number;
  showHorizon: boolean;
}
```

默认值：`latitude: 0, longitude: 0`（赤道、本初子午线）

### 5.2 经纬度范围

- 纬度范围：`[-π/2, π/2]` 弧度 → 北纬为正 ✓
- 经度范围：`[-π, π]` 弧度 → 东经为正 ✓

约定与 `MapPanel.tsx` 一致：
```ts
// MapPanel.tsx:398-399
const lonDeg = (x / imgWidth) * 360 - 180  // → [-180, 180]
const latDeg = 90 - (y / imgHeight) * 180   // → [90, -90]
```

### 5.3 海拔高度

**无。** 当前所有计算假设观测者位于行星表面（`planetRadius` 作为地表半径）。`SurfaceObservationState` 中没有海拔字段。

### 5.4 用户选点方式

通过 `MapPanel.tsx` 的**点击**交互（等距矩形投影反算）。不支持搜索或拖拽 Marker。

### 5.5 观察点变化时的相机行为

**瞬间跳转。** 当用户在地图面板点击新位置后，地表视角的相机方向（yaw/pitch）被重置为 `(0, 0)`，纬度和经度直接更新到 store。没有平滑过渡动画。

### 5.6 "本地正午"/"地方时"概念

**已存在（astronomy.ts 层面）**。`calcLST(T, lonRad)` 基于观察者经度计算地方恒星时。`equatorialToHorizontal()` 将赤道坐标转为地平坐标，正确反映了不同经度的时角差异。

但在 `DetailPage.tsx` 的 PlanetMesh 中仍使用旧的简化方法：
```ts
// DetailPage.tsx:416-417 — 未升级
const angle = detailPageState.dayTime * Math.PI * 2
```

---

## 6. 现有渲染对象审计

### 6.1 主场景（Scene.tsx → App.tsx）

| 对象名 | Three.js 类型 | 数据来源 | 备注 |
|--------|-------------|----------|------|
| 恒星本体 | `Mesh` (SphereGeometry) + `PointLight`×2 | `celestialBodies` store | `meshBasicMaterial`，固定原点 (0,0,0)；PointLight distance=500/300，decay=0 |
| 恒星光晕 | `Mesh`×4 (BackSide 球壳) | `celestialBodies` store + `starGlow` | 多层 scale (1.3x~3.2x) 半透明光晕，`AdditiveBlending` |
| 行星本体 | `Mesh` (SphereGeometry 128×128) | `celestialBodies` + `sizeScale` + 贴图/程序化纹理 | `meshStandardMaterial`，自转通过 `meshRef.current.rotation.y`，使用 `timeSystem.T` 增量 |
| 行星大气 | `Mesh`×2 (BackSide 球壳) | `showAtmosphere` flag | scale 1.05x/1.1x，`BasicMaterial`，仅 `earth-like` 类型 |
| 行星环 | `Mesh`×3 (RingGeometry) + `Points`(200粒子) | `body.hasRing` + `ringColor` + `showRings` | 内环(1.4r-1.8r)/中环(1.8r-2.1r)/外环(2.1r-2.4r)，`MeshStandardMaterial`，绕 Z 轴自转 |
| 轨道线 | `Line` (BufferGeometry) | `orbitalElements` → `getOrbitPointsFromT` (128 段，开普勒椭圆) | 降级：圆形（`cos(angle)*radius, 0, sin(angle)*radius`）→ Y=0 平面 |
| 卫星本体 | `Mesh` (SphereGeometry 32×32) | `celestialBodies` | 绕父行星公转，`useFrame` 更新位置 |
| 星空背景 | `Stars` (drei) | 硬编码 | radius=150, depth=80, count=8000, factor=5 |
| 星云 | `Mesh`×3 (SphereGeometry BackSide) | `showNebula` | 紫色(#4a148c)/蓝色(#0d47a1)/红色(#b71c1c)，半透明 |
| 小行星带 | `Points` (1500 粒子) | 硬编码 | innerRadius=45, outerRadius=55, Y≈0 平面，每帧更新轨道角 |
| 尘埃云 | `Points` (800 粒子) | 硬编码 | radius=70, thickness=25, 球壳分布，缓慢自转 |

### 6.2 地表视角场景（SurfaceView.tsx）

| 对象名 | Three.js 类型 | 数据来源 | 备注 |
|--------|-------------|----------|------|
| 天空穹顶 | `Mesh` (SphereGeometry, BackSide) + `ShaderMaterial` | sunDirection/sunAltitude（来自 `sunEclipticHigh` + `eclipticToEquatorial` + `equatorialToHorizontal` + `horizontalToWorldDir`） | 昼夜三段过渡着色器（白天→黄昏→黑夜） |
| 太阳 | `Billboard` + CircleGeometry | sunDirection/altitude | 高度角<−6° 时隐藏，含光晕 3 层，Html 标注"太阳" |
| 星空 | `Points` (3000 粒子) | 随机生成 | opacity 随 sunAltitude 变化（白天淡/夜晚亮） |
| 星座连线 | `LineSegments` (50 对随机线) | 随机生成 | 可选显示 |
| 黄道线 | `Line` (100 段) | sunDirection → applyAxisAngle(orbitNormal, angle) | 绕 `orbitNormal = (0, cosTilt, sinTilt)` 旋转 360° |
| 地平线 | `Line` (100 段，Y=0 圆) | 硬编码 | radius=800, Y=0 平面 |
| 方向标识 | Cone(北)/Box(东)/Torus(南)/Sphere(西) | 硬编码 | [见下方 DirectionMarkers 坐标分析] |
| 天体标记 | `Billboard` + CircleGeometry | 开普勒轨道 → `worldToSkyPosition` → `altAzToDirection` | 卫星/外行星，含光晕 + Html 名称标签，相位角和距离衰减 |
| 天体轨迹 | `Line` (80 段) | 模拟采样：太阳年轨迹/卫星周期轨迹/外行星年视运动 | 含地表投影过滤（altitude < -0.1 跳过） |
| 行星环弧 | `Mesh`×3 (RingGeometry) | `hasRing` | 地表视角下的环带投影，旋转角 = π/2 - latitude + axialTilt |
| 地面 | `Mesh` (PlaneGeometry) + `ShaderMaterial` | sunDirection + 贴图采样色 | 昼夜阴影，DepthWrite=false |
| 大气辉光 | `Mesh`×2 (ConeGeometry + PlaneGeometry) | sunDirection | AdditiveBlending 散射效果 |
| 相机 | `PerspectiveCamera` (Canvas 内置) + 自定义 `CameraController` | yawRef/pitchRef | 手动控制旋转 |

### 6.3 DirectionMarkers 方向标识分析

`SurfaceView.tsx:367-374` 中 DirectionMarkers 的位置来自 `enuBasis.north` 和 `enuBasis.east` 向量，**由 ENU 局部坐标系动态计算**（而非硬编码）。这是相对上一版审计的改进。

但存在一个潜在问题：
```ts
// SurfaceView.tsx:371-374 — DirectionMarkers
const directions = useMemo(() => [
  { pos: north.clone().multiplyScalar(markerRadius), color: '#ff6b6b', type: 'cone' },      // 北 - 锥形
  { pos: east.clone().multiplyScalar(markerRadius), color: '#4ecdc4', type: 'box' },        // 东 - 盒子
  { pos: north.clone().multiplyScalar(-markerRadius), color: '#45b7d1', type: 'ring' },     // 南 - 圆环
  { pos: east.clone().multiplyScalar(-markerRadius), color: '#96ceb4', type: 'sphere' },    // 西 - 球体
], [north, east])
```

这些标记在 Y 方向有 +50 的偏移（L380：`position={[dir.pos.x, dir.pos.y + 50, dir.pos.z]}`），这使它们在非赤道纬度仍可见，但方向指示的准确性会随纬度升高而偏移。这是 UI 层面的简化。

### 6.4 详细信息页 3D 场景（DetailPage.tsx）

| 对象名 | Three.js 类型 | 数据来源 | 备注 |
|--------|-------------|----------|------|
| 行星本体 | `Mesh` (SphereGeometry) + `ShaderMaterial` | customTexture + dayNightShader | 昼夜着色器：sunDot → brightness(0.25~1.0) + 阴影贴图(PCF 4×4) |
| 云层 | `Mesh` (SphereGeometry 1.01x) + `ShaderMaterial` | cloudTexture | 晨昏线过渡 + 夜晚半透明 |
| 大气内层+外层 | `Mesh` (SphereGeometry) + `ShaderMaterial` × 2 | atmosphereColor/density | fresnel 效果（BackSide + AdditiveBlending） |
| 辉光 | `Mesh` (SphereGeometry) + `ShaderMaterial` | atmosphereOuterRadius | 最外层辉光 |
| 行星环 | `Mesh`×3 (RingGeometry) + `ShaderMaterial` | ringColor + sunDirection | 实时光照：基于环面方位角计算 brightness |
| 环粒子 | `Points` | ringParticleCount(500) | 可选单独显示 |
| 定向光源 | `DirectionalLight` + shadow camera | sunDirection(从 dayTime 派生) | shadow map 4096×4096，PCF 软阴影 |

### 6.5 特别说明

**行星环旋转方式（Planet.tsx）**：
- `Planet.tsx:89`：`tiltGroupRef` 旋转 `[0, 0, axialTilt]`
- `Planet.tsx:92`：`ringGroupRef` 旋转 `[-Math.PI / 2.5, 0, 0]`（约 72°）
- 自转时绕 Z 轴旋转（`ringGroupRef.current.children[0].rotation.z`）
- 环旋转与 `keplerOrbit.ts` 中轨道面在 X-Z 平面的约定一致

---

## 7. 状态流与数据流分析

### 7.1 统一时间 → 天体位置更新（改进后）

```
用户拖动 timeScale 滑块 或 R3F useFrame tick
    ↓
Zustand store: timeSystem.T += delta * timeScale / SECONDS_PER_DAY  [非暂停时]
    ↓
Scene.tsx useFrame: T = store.getState().timeSystem.T
    ↓
遍历 celestialBodies:
    star   → position = (0, 0, 0)
    planet → calculateOrbitalPositionFromT(T, orbitalPeriodDays, orbitalElements)
           → x * distanceScale, y * distanceScale, z * distanceScale
    moon   → 同 planet + parentPos offset
    ↓
直接操作 Three.js 对象：
    bodyRefs.get(id).position.copy(pos)  ← NO React state update!
    ↓
Star 组件独立于时间流：
    PointLight 固定在原点 (0,0,0)
```

### 7.2 地表视图天文计算链路（SurfaceView.tsx，已升级）

```
timeSystem.T 变化
    ↓
sunEclipticHigh(T) → [λ, ε′] （太阳视黄经 + 真黄赤交角）
    ↓
eclipticToEquatorial(λ, 0, ε′) → [α, δ] （赤经、赤纬）
    ↓
calcLST(T, longitude) → lst （地方恒星时）
    ↓
equatorialToHorizontal(α, δ, lst, latitude) → [h, A] （高度角、方位角）
    ↓
horizontalToWorldDir(h, A, up, east, north) → sunDirection （世界方向）
    ↓
sunDirection/sunAltitude → SkyDome, StarField, Ground, CelestialMarkers, ...
```

### 7.3 详情页寰球昼夜着色器（DetailPage.tsx PlanetMesh，未升级）

```
detailPageState.dayTime 变化  ← 仍独立于 timeSystem.T
    ↓
PlanetMesh useFrame: angle = dayTime * 2π
    ↓
sunRef.current.set(sin(angle), 0, cos(angle))  ← Y=0 平面简谐运动
    ↓
→ 更新 dayNightShader uniforms
→ 更新云层/环 shader uniforms
→ 更新行星自转 rotation.y
```

### 7.4 用户交互 → Three.js 对象更新

```
ControlPanel:
    滑块/按钮 → zustand updateTimeSystem({T, timeScale, isPaused}) 或 setXxx()
    ↓ (Zustand 响应式)
Scene useFrame: 读取 timeSystem.T/timeScale/isPaused/showOrbits 等
Planet/Moon: 读取 distanceScale/sizeScale + timeSystem.T
Star: 读取 brightness/lightIntensity/starGlow
InfoPanel: 读取 selectedBody → 显示/编辑

DetailPage (行星细节页):
    设置面板滑块 → updateDetailPageState()
    → PlanetMesh useMemo/useFrame 自动响应
    → SurfaceView props 变化 → 重渲染
```

### 7.5 useFrame 使用分析

| 组件 | useFrame 中做什么 | 性能影响 |
|------|-------------------|----------|
| `Scene.tsx` | 累加 T、计算所有天体位置、直接操作 bodyRefs.position | **高** — 每帧更新 7-8 个天体 + 3 个卫星 |
| `Planet.tsx` | 读取 T、计算自身轨道位置、设置 groupRef.position、自转、环自转、计算 sunDirection | 中高 — 每帧 3 次 store.getState() |
| `Moon.tsx` | 读取 T、计算轨道位置、设置 groupRef.position、自转 | 中 |
| `AsteroidBelt.tsx` | 更新 1500 个粒子轨道角 | 中高 — 直接修改 BufferGeometry |
| `DustCloud.tsx` | 整体旋转 | 低 |
| `DetailPage > PlanetMesh` | 从 dayTime 更新 sunDirection + 多种 shader uniforms + 自转 + 云层旋转 | 中高 |
| `SurfaceView > CameraController` | 更新相机 fov + rotation | 低 |
| `SurfaceView > SkyDome` | 更新 shader uniforms (sunDirection, sunAltitude) | 低 |
| `SurfaceView > Ground` | 更新 shader uniforms | 低 |
| `SurfaceView > StarField` | 慢速旋转 | 低 |

### 7.6 数据流 ASCII 图

```
┌─────────────────────────────────────────────────┐
│                  Zustand Store                    │
│  ┌──────────┐  ┌────────────┐  ┌──────────────┐ │
│  │TimeSystem│  │celestial   │  │detailPage    │ │
│  │ .T       │  │Bodies[]    │  │State         │ │
│  │ .timeScl │  │            │  │ .dayTime     │ │
│  │ .isPaused│  │            │  │ .surfaceObs  │ │
│  │distanceSz│  │            │  │ .globalTime  │ │
│  └────┬─────┘  └─────┬──────┘  └──────┬───────┘ │
└───────┼──────────────┼────────────────┼─────────┘
        │              │                │
        ▼              ▼                ▼
   ┌─────────┐   ┌──────────┐   ┌──────────────┐
   │ Scene   │   │ Planet   │   │ DetailPage   │
   │ useFrame│   │ Star     │   │  ┌─────────┐ │
   │  ↓      │   │ Moon     │   │  │Planet   │ │
   │直接ref  │   │ Orbit    │   │  │Mesh     │ │
   │操作3D   │   │ Asteroid │   │  │(dayTime)│ │
   │对象     │   │ DustCloud│   │  ├─────────┤ │
   └─────────┘   └──────────┘   │  │Surface  │ │
                                 │  │View     │ │
                                 │  │(T驱动)  │ │
                                 │  └─────────┘ │
                                 └──────────────┘
```

---

## 8. 矛盾点与风险标记

### 8.1 🔴 高危 — 详情页 PlanetMesh 时间未统一

`DetailPage.tsx` 的寰球视角（PlanetMesh）仍使用 `detailPageState.dayTime`（0-1 比例）驱动太阳方向，而地表视图已迁移到 `timeSystem.T`。**同一页面的两个视图使用不同的时间系统和不同的太阳位置算法**。

```ts
// DetailPage PlanetMesh useFrame — 简化算法
const angle = detailPageState.dayTime * Math.PI * 2
sunRef.current.set(Math.sin(angle), 0, Math.cos(angle))  // Y=0 平面旋转
```

对比 SurfaceView 使用的正确算法：
```ts
// SurfaceView.tsx — 基于 astronomy.ts 的完整链路
const [sunLon, epsPrime] = sunEclipticHigh(T)
const [sunRA, sunDec] = eclipticToEquatorial(sunLon, 0, epsPrime)
const lst = calcLST(T, longitude)
const [h, A] = equatorialToHorizontal(sunRA, sunDec, lst, latitude)
const sunDirection = horizontalToWorldDir(h, A, up, east, north)
```

**影响**：在详情页切换到地表视角和轨道视角时，太阳位置会跳变。PlanetMesh 的太阳方向不考虑轴倾角和行星轨道位置。

### 8.2 🟡 中高危 — `astronomy.ts` 的太阳视黄经含有章动+光行差，但未含岁差

```ts
// astronomy.ts:67-90 — sunEclipticHigh
// 包含：平黄经 + 中心差 + 章动Δψ + 光行差ΔA
// 不包含：岁差（注释："不含岁差项"）
```

这合理——岁差是长期效应（~50.3 角秒/年），在时间跨度较小的模拟中可忽略。但**文档应明确标注**。

### 8.3 🟡 中危 — `DetailPage.tsx` 中 `setInterval` 驱动 `globalTime`

`DetailPage.tsx` 中的 `setInterval(16ms)` 每帧向 store 写入 `globalTime/dayTime/yearTime`。这是独立于 `Scene.tsx` useFrame 的第二时间更新源。虽然 `dayTime` 已被标记 `@deprecated`，但代码路径仍然活跃（PlanetMesh 仍在使用）。

### 8.4 🟡 中危 — `calendarFromT` 的大 while 循环

```ts
// calendar.ts:45-56
export function calendarFromT(T: number): CalendarDate {
  let remaining = T
  let year = 1
  while (true) {           // ← 无限循环条件
    const len = yearLength(year)
    if (remaining < len) break
    remaining -= len
    year++
  }
```

对于大 T 值（模拟多年后），此循环会执行数千次。建议改为数学闭式解（floor(T / yearLength_norm)）。

### 8.5 🟡 中危 — ENU 坐标系在实际应用中不一致

`surfaceCoords.ts` 和 `SurfaceView.tsx` 各自独立构建 ENU 局部坐标系，逻辑几乎完全相同但未复用。如果修改 ENU 约定，需要两处同步更新。

#### `surfaceCoords.ts:158-176`（worldToSkyPosition 中的 ENU）：
```ts
const up = new THREE.Vector3(worldX, worldY, worldZ).normalize()
const rotationAxis = new THREE.Vector3(0, cosTilt, sinTilt)
let east = new THREE.Vector3().crossVectors(rotationAxis, up).normalize()
if (east.length() < 0.001) { east = new THREE.Vector3(1, 0, 0); /* 投影到切平面 */ }
const north = new THREE.Vector3().crossVectors(up, east).normalize()
```

#### `SurfaceView.tsx:1101-1134`（SurfaceViewScene 中的 ENU）：
```ts
// 完全相同的逻辑，但作为独立代码块
const up = new THREE.Vector3(worldX, worldY, worldZ).normalize()
const rotationAxis = new THREE.Vector3(0, cosTilt, sinTilt)
let east = new THREE.Vector3().crossVectors(rotationAxis, up).normalize()
if (east.length() < 0.001) { east = new THREE.Vector3(1, 0, 0); /* ... */ }
const north = new THREE.Vector3().crossVectors(up, east).normalize()
```

### 8.6 🟡 中危 — 天文模块的多精度函数并存但未统一入口

`astronomy.ts` 提供了三级太阳视黄经（Low/Medium/High），但 `surfaceCoords.ts` 中的旧函数 `calculateSunSkyPosition` 和 `SurfaceView.tsx` 中的 `EclipticLine` 各自使用不同的方法。应统一切换到 `sunEclipticHigh`。

### 8.7 🟢 低危 — `Scene.tsx` 不再使用 React state 更新天体位置

上一版审计报告标记的 "Scene.tsx 每帧 setState" 问题**已修复**。当前代码使用 `bodyRefs` Map 直接操作 Three.js 对象：

```ts
// Scene.tsx:77-81 — 改进
positions.forEach((pos, id) => {
  const obj = bodyRefs.current.get(id)
  if (obj) obj.position.copy(pos)
})
```

### 8.8 🟢 低危 — 硬编码常量散落

| 常量 | 出现位置 | 说明 |
|------|----------|------|
| `SECONDS_PER_DAY = 24.15 * 3600` | `constants.ts` (唯一定义) | 已收敛到 config，但与 `DetailPage.tsx` 中的 `DAY_IN_SECONDS` 重复 |
| `D_YEAR = 426.15` | `constants.ts` (唯一定义) | 已收敛 |
| `orbitalPeriodDays` | store（天体数据） | 每个天体显式指定，取代了之前的 `basePeriod=50/30` |
| `axialTilt` | store（天体数据） + `DetailPage.tsx` 的 `detailPageState.axialTilt` | 宜居行星=0.33rad(19°)，但 DetailPage 的 `axialTilt` 为 0.41rad(23.5°)——应统一 |
| 行星环内外半径 1.4r-2.4r | `Planet.tsx` + `DetailPage.tsx` | 两处独立定义 |
| 星空 count = 8000/3000 | `App.tsx` (8000) + `SurfaceView.tsx` (3000 StarField 独立实现) | 合理，主场景和地表视图的星空需求不同 |

### 8.9 🟢 低危 — 角度/弧度一致性

代码整体使用弧度，但 `MapPanel.tsx` 在 UI 交互中使用度数并在回调时转换。`InfoPanel.tsx` 编辑界面中也进行度/弧度转换。整体一致，无明显混用。

### 8.10 🟢 低危 — `createSimpleOrbit` 生成随机 `meanAnomaly` 和 `argumentOfPeriapsis`

```ts
// keplerOrbit.ts:139-148
export function createSimpleOrbit(distance, eccentricity=0, inclination=0): OrbitalElements {
  return {
    semiMajorAxis: distance,
    eccentricity,
    inclination,
    longitudeOfAscendingNode: 0,
    argumentOfPeriapsis: Math.random() * Math.PI * 2,  // 随机！
    meanAnomaly: Math.random() * Math.PI * 2,           // 随机！
  }
}
```

每次刷新页面，天体的初始轨道位置会随机变化。这对于演示可能没问题，但对于精确的天文计算，应使用确定性初始值。

---

## 9. 附件：关键代码片段

### 9.1 时间更新（Scene.tsx — 已统一）

```ts
// Scene.tsx:22-26
useFrame((state, delta) => {
  if (!timeSystem.isPaused) {
    const dtDays = (delta * timeSystem.timeScale) / SECONDS_PER_DAY
    updateTimeSystem({ T: timeSystem.T + dtDays })
  }
  const T = useStore.getState().timeSystem.T
  // ... 使用 T 计算所有天体位置 ...
})
```

### 9.2 轨道位置计算（keplerOrbit.ts — 传入显式周期）

```ts
// keplerOrbit.ts:90-109
export function calculateOrbitalPositionFromT(
  T: number,
  orbitalPeriodDays: number,
  elements: OrbitalElements
): { x: number; y: number; z: number } {
  const meanAnomaly = (elements.meanAnomaly + (2 * Math.PI * T) / orbitalPeriodDays) % (2 * Math.PI)
  const eccentricAnomaly = solveKeplerEquation(meanAnomaly, elements.eccentricity)
  const trueAnomaly = 2 * Math.atan2(
    Math.sqrt(1 + elements.eccentricity) * Math.sin(eccentricAnomaly / 2),
    Math.sqrt(1 - elements.eccentricity) * Math.cos(eccentricAnomaly / 2)
  )
  return convertOrbitalToCartesian(trueAnomaly, elements)
}
```

### 9.3 天文太阳视位置计算链路（astronomy.ts — 高精度）

```ts
// astronomy.ts:67-90 — sunEclipticHigh
export function sunEclipticHigh(T: number): [number, number] {
  const e = ECCENTRICITY;
  const M = (2 * Math.PI) * T / D_YEAR;
  const C = 2 * e * Math.sin(M) + (5/4) * e * e * Math.sin(2 * M);
  const deltaPsi = A_N_ARCSEC * ARCSEC2RAD * Math.sin((2*Math.PI)*T/(P_N_YEARS*D_YEAR));
  const deltaA = DELTA_A_ARCSEC * ARCSEC2RAD;
  const lam = mod2pi(M + C + deltaPsi - deltaA);
  const deltaEpsilon = deltaPsi * Math.cos(EPSILON_RAD);
  const epsPrime = EPSILON_RAD + deltaEpsilon;
  return [lam, epsPrime];
}

// 黄道→赤道→地平完整链路
// astronomy.ts:101-118 — eclipticToEquatorial
// astronomy.ts:128-148 — equatorialToHorizontal
// astronomy.ts:203-222 — horizontalToWorldDir
```

### 9.4 轨道坐标 → 三维空间（keplerOrbit.ts）

```ts
// keplerOrbit.ts:52-80 — convertOrbitalToCartesian
// 轨道平面内的坐标
const xOrbit = r * Math.cos(trueAnomaly)
const zOrbit = r * Math.sin(trueAnomaly)  // X-Z 平面（Y=0）

// 欧拉角变换（Ω, ω, i）
const x = xOrbit * (cosΩ*cosω - sinΩ*sinω*cosi) - zOrbit * (cosΩ*sinω + sinΩ*cosω*cosi)
const z = xOrbit * (sinΩ*cosω + cosΩ*sinω*cosi) - zOrbit * (sinΩ*sinω - cosΩ*cosω*cosi)
const y = xOrbit * sinω*sini + zOrbit * cosω*sini
```

### 9.5 经纬度 → 世界坐标（完整链路，surfaceCoords.ts）

```ts
// surfaceCoords.ts:106-193 — worldToSkyPosition（核心 88 行）
// 第1步：局部坐标（经度 0°=+Z, 90°=+X）
const localX = planetRadius * cosLat * sinLon   // 东西
const localY = planetRadius * sinLat              // 南北/极轴
const localZ = planetRadius * cosLat * cosLon    // 前后

// 第2步：自转 + 轴倾角
const rotX = localX*cosRot + localZ*sinRot
const rotZ = -localX*sinRot + localZ*cosRot
const worldY = rotY*cosTilt - rotZ*sinTilt
const worldZ = rotY*sinTilt + rotZ*cosTilt

// 第3步：观测点世界坐标
const surfacePos = new THREE.Vector3(
  planetCenterPos.x + rotX, planetCenterPos.y + worldY, planetCenterPos.z + worldZ)

// 第4步：ENU 局部坐标系
const up = normalize(surfaceOffset)                    // 天顶
const east = normalize(rotationAxis × up)             // 东
const north = normalize(up × east)                    // 北（右手术）

// 第5-6步：方向投影 → 高度角/方位角
```

### 9.6 相机初始化与控制器配置

```ts
// App.tsx:112,136-144（主场景 — 太空轨道视角）
const cameraPosition = useMemo(() =>
  [0, 30 * distanceScale, 50 * distanceScale], [distanceScale])
<Canvas camera={{ position: cameraPosition, fov: 60 }}
  gl={{ antialias: true, powerPreference: "high-performance" }} shadows>
  <OrbitControls enableDamping dampingFactor={0.05}
    minDistance={5*sizeScale} maxDistance={200*distanceScale} />
</Canvas>

// DetailPage.tsx（详情页轨道视角 — 近地轨道）
<Canvas camera={{ position: [0, 0, 6], fov: 60 }} shadows>
  <OrbitControls enableDamping minDistance={3} maxDistance={20} />
</Canvas>

// SurfaceView.tsx（地表视角 — 第一人称）
<Canvas camera={{ position: [0, 0.1, 0], fov, near: 0.01, far: 2000 }}>
  <CameraController fov={fov} yawRef={yawRef} pitchRef={pitchRef} />
</Canvas>
```

### 9.7 观察点选择事件链

```ts
// MapPanel.tsx:384-403 — 地图点击 → 经纬度
const handleMapClick = (e) => {
  const x = e.clientX - rect.left
  const y = e.clientY - rect.top
  const lonDeg = (x / imgWidth) * 360 - 180
  const latDeg = 90 - (y / imgHeight) * 180
  onSelectPoint?.(latDeg * Math.PI / 180, lonDeg * Math.PI / 180)
}

// DetailPage.tsx:970-972 — 观察点更新
onSelectPoint={(lat, lon) => {
  setSurfaceObservation({ latitude: lat, longitude: lon })
}}
```

### 9.8 降级太阳位置（Scene.tsx — 仅后备路径）

```ts
// Scene.tsx:45-49 — 当无 orbitalElements 或无 orbitalPeriodDays 时
} else if (planet.distance && planet.orbitSpeed) {
  const angle = T * planet.orbitSpeed * 0.1
  const x = Math.cos(angle) * planet.distance * distanceScale
  const z = Math.sin(angle) * planet.distance * distanceScale
  positions.set(planet.id, new THREE.Vector3(x, 0, z))
}
```

**注意**：当前所有天体数据均配置了 `orbitalElements` 和 `orbitalPeriodDays`，因此此降级路径实际上不会被触发。但代码仍然存在。

### 9.9 详情页 PlanetMesh 太阳方向（未升级）

```ts
// DetailPage.tsx ~416-417 — PlanetMesh useFrame
const angle = detailPageState.dayTime * Math.PI * 2
sunRef.current.set(Math.sin(angle), 0, Math.cos(angle))
```

---

## 10. 总结：天文模型 v3.1 集成就绪度评估

| 评估维度 | 就绪度 | 说明 |
|----------|--------|------|
| 坐标系 | **高** | 已有明确的 Y-up + X-Z 轨道面约定，astronomy.ts 提供了完整的黄道→赤道→地平→世界方向转换链 |
| 时间系统 | **中高** | 已统一为 `timeSystem.T`（本地日），但 DetailPage PlanetMesh 的太阳方向仍使用旧 dayTime |
| 轨道计算 | **高** | `keplerOrbit.ts` 实现了完整的开普勒轨道求解（牛顿迭代 + 欧拉角变换 + 显式周期） |
| 天文计算 | **高** | `astronomy.ts` 提供 sunEclipticHigh/calcLST/eclipticToEquatorial/equatorialToHorizontal 等纯函数，接口与 v3.1 需求高度吻合 |
| 地表天象 | **高** | `worldToSkyPosition()` + `horizontalToWorldDir()` 实现完整的天体→天空投影，SurfaceView.tsx 已迁移到 astronomy.ts 链路 |
| 渲染管线 | **高** | R3F + ShaderMaterial 架构灵活，新天文对象可独立组件注入 |
| 状态管理 | **高** | Zustand 单 store，`timeSystem.T` 为全局唯一时间源，直接 ref 操作避免 React state 污染 |
| 观察点系统 | **中** | 经纬度数据完备，ENU 框架正确，缺乏海拔和过渡动画 |
| 代码重复 | **中** | ENU 构建和 DAY_IN_SECONDS 等存在重复代码，需提取为公共函数 |

### 最关键的集成步骤建议（优先级排序）

1. **🔴 立即**：统一 DetailPage PlanetMesh 的太阳方向计算。将 `dayTime` 驱动替换为 `timeSystem.T` → `sunEclipticHigh` → `eclipticToEquatorial` 链路，同时考虑轴倾角。

2. **🔴 立即**：删除或废弃 `detailPageState.globalTime/dayTime/yearTime`，统一所有时间相关计算到 `timeSystem.T`。

3. **🟡 建议**：将 ENU 局部坐标系构建提取为独立工具函数 `buildENUBasis(lat, lon, planetRotation, axialTilt)`，供 `surfaceCoords.ts` 和 `SurfaceView.tsx` 共享。

4. **🟡 建议**：将 `calendarFromT` 中的 while 循环改为数学闭式解。

5. **🟢 可选**：统一 `createSimpleOrbit` 使用确定性种子（如 `body.id` 哈希）替代 `Math.random()`。

6. **🟢 可选**：提取公共常量（`SECONDS_PER_DAY` / `DAY_IN_SECONDS`、环半径比例、星空参数等）到单一定义文件。

---

**审计完成。此报告可直接提交给天文模型集成专家进行 v3.1 规格书对照和专业化集成方案设计。**